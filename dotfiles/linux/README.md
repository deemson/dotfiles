# linux

Linux-specific configuration (extends posix).

- **ghostty** — terminal emulator
- **sway** — tiling window manager for Wayland
- **waybar** — status bar for Wayland compositors
- **wofi** — application launcher for Wayland
